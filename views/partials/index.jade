.row
  .col-lg-7
    div(ng-repeat='post in posts')
      h3
        a(href='/readPost/{{post.id}}') {{post.title}}
      h5 {{post.author}}
      div {{post.text}}
      p
      .btn-group.btn-group-sm
        a(href='/readPost/{{post.id}}').btn.btn-default Read More
        a(href='/editPost/{{post.id}}').btn.btn-default Edit
        a(href='/deletePost/{{post.id}}').btn.btn-default Delete
      p 
  .col-lg-1
  .col-lg-4
    h2 Sidebar
    p
    | There are a total of {{posts.length}} posts.